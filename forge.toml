[package]
name = "scroll"
version = "0.1.0"
type = "tool"
description = "Interactive notebook for March — Livebook-style in the browser"
author = ""

[deps]
bastion = { path = "/Users/80197052/code/bastion" }

[archive]

[archive.task.scroll.serve]
command = "scroll.serve"
module  = "lib/scroll.march"
